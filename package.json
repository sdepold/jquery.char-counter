{
  "author": "Sascha Depold <sascha@dawanda.com> (http://depold.com)",
  "name": "jquery.char-counter",
  "description": "A jQuery plugin for counting and limiting the characters in a textarea.",
  "version": "0.0.0",
  "homepage": "https://github.com/dawanda/jquery.char-counter",
  "repository": {
    "type": "git",
    "url": "git://github.com/dawanda/jquery.char-counter.git"
  },
  "scripts": {
    "test": "java -jar dist/compiler.jar src/*.js --js_output_file=dist/`pwd|sed -e 's/.*jquery\\.//'`.min.js && node_modules/.bin/buster-test"
  },
  "dependencies": {
    "buster": "~0.5.0"
  },
  "devDependencies": {},
  "optionalDependencies": {},
  "engines": {
    "node": "*"
  }
}
